repos:
  - repo: local
    hooks:
      - id: pytest-fast
        name: pytest (fast unit tests)
        entry: python -m pytest tests/ -m "fast" --tb=short
        language: system
        pass_filenames: false
        always_run: true
